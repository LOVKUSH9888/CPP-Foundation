#include<bits/stdc++.h>
#include<iostream>
using namespace std;


// for e.g. array = {2, 2, 2, 1, 1, 4, 3, 4};
///Here key(elements) = 2, 1, 4, 3 
///Value(frequency) = 3, 2, 2, 1 {how many time the elemets comes}


int main(){

     //Declaration & taking input

    int n;
    cin>>n;
    int arr[n];
    for(int i=0; i<n; i++){
        cin>>arr[];
    }

    //Taking Queries from the user

    int q;
    cin>>q;
    while(q--){
        int number;
        cin>>number;
    }


}

